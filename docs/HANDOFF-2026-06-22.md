# Scribe Handoff Snapshot: 2026-06-22

This dated snapshot mirrors `docs/HANDOFF-codex.md` after the provider/configuration cleanup and full verification pass.

Read `docs/HANDOFF-codex.md` first. It is the canonical current handoff.

Key status:

- Branch: `codex/generic-record-architecture`
- Remote alias after cleanup: `scribe`
- Remote URL: `https://github.com/DECADE0502/Scribe.git`
- Runtime provider verified: `xiongmao`
- Runtime provider Base URL: `https://api520.pro/v1`
- Runtime auth mode: Bearer
- Runtime write/audit model: `gemini-2.5-pro`

Latest verification:

```text
server typecheck: passed
client typecheck: passed
server tests: 92 files / 529 tests passed
client tests: 24 files / 110 tests passed
runtime /api/models: returned model IDs successfully
```

Directory cleanup:

- moved `.superpowers/sdd/*` into `docs/superpowers/sdd/2026-06-21-agent-workflow-execution/`;
- removed the empty `.superpowers` directory;
- kept implementation artifacts under `docs` so future agents can inspect the execution/review trail without hidden root clutter.

Most recent fix:

- Custom OpenAI-compatible providers now work even when the user enters a Chinese display name and leaves the ASCII provider ID blank.
- Model list failures now include requested URL and cause, without exposing API keys.

Security note:

The user pasted provider keys into chat during debugging. They are not committed by this repo cleanup, but the user should rotate exposed keys.
