# Scribe Samples

这里保存用于本地验证和复现的输入样例。样例不是产品架构的定义,只能用来暴露问题。

## 目录

- `sillytavern/`
  - `Izumi 0503.json`: SillyTavern 预设样例。
  - `宠物捕捉系统-世界书.json`: SillyTavern 世界书样例。

## 使用原则

- 可以用这些文件验证导入、预设渲染、世界书检索和长篇连续性。
- 不允许把样例里的专有词、角色名、道具名写死进通用逻辑。
- 如果新增样例,要放在对应子目录,并在 README 说明用途。
