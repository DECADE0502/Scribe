- **后端运行时**：Node.js (ESM) + Hono (HTTP 框架) + better-sqlite3 (嵌入式数据库)。
- **AI 集成**：Vercel AI SDK (`ai` 库) 用于模型抽象与流式处理，支持 DeepSeek 与 MiMo 等 OpenAI 兼容接口。
- **前端技术栈**：React 18 + Vite + Zustand (状态管理) + Tiptap (富文本编辑器) + React Router 7。
- **测试与监控**：Vitest (单元测试) + Playwright (E2E 测试) + Pino (日志记录)。